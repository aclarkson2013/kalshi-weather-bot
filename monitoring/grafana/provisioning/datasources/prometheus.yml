# Auto-provision Prometheus as the default Grafana datasource.
#
# Grafana reads this on startup and creates the datasource automatically.
# No manual configuration needed — just docker compose up.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
